{
  "graphs": {
    "spec_discovery": {
      "path": "solver_coworker.graphs.spec_discovery_graph:build_graph"
    }
  },
  "dependencies": [
    "requirements.txt"
  ],
  "env": {
    "OPENAI_API_KEY": "${OPENAI_API_KEY}",
    "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
  }
}
